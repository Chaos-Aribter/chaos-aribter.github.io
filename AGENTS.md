# 项目执行约束

每次开始本项目任务时，先读取 `docs/NEWS_GUIDELINES.md`。涉及新闻数据、首页新闻入口、Banner、新闻列表、详情模板、图片、排版或相关共享样式时，必须按该文档实施并逐项验收。

- 用户当前明确要求优先于本文与规范；出现新要求时同步维护规范，不静默覆盖约束。
- 修改前检查已有改动，保留用户和其他任务的工作。
- 新闻必须沿用本站黑底、米白文字、红色强调和栅格风格；响应式以容器可用宽度为准。
- 文档记录的是目标规范，不代表相关后台、裁图、定时发布或统计能力已经实现。实施前核实代码，交付时如实说明缺口。
- 默认仅本地修改和验证。只有用户明确验收并授权发版后才提交发布或推送远端；不得因构建通过自动发布。
